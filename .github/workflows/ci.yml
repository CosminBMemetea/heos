name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build dev image
        run: docker build -t heos-dev -f Dockerfile.dev .
      - name: Run tests
        run: docker run --rm -e CI=true heos-dev npm test
